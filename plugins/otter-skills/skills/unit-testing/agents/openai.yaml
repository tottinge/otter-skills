interface:
  display_name: "Unit Testing"
  short_description: "Build behavior test-first with fast microtests."
  default_prompt: "Use $unit-testing to protect evidence-backed behavioral rules with safe, isolated microtests in small red-green-refactor cycles."
policy:
  allow_implicit_invocation: true
