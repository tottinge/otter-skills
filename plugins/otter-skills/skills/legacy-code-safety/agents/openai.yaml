interface:
  display_name: "Legacy Code Safety"
  short_description: "Find and protect legacy code's real decisions."
  default_prompt: "Use $legacy-code-safety to locate the real decisions, contain reachable effects, and prove a trustworthy test boundary before changing this legacy code."
policy:
  allow_implicit_invocation: true
